// 部分和問題に対するビットを用いる全探索解法

#include <bits/stdc++.h>
using namespace std;

int main(){
    int N, W;
    cin >> N >> W;
    
    vector<int> a(N);
    for(int i=0; i<N; i++) cin >> a[i];

    return 0;
}
